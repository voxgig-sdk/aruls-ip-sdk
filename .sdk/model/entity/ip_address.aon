# Entity: ip_address

main: kit: entity: ip_address: {

  alias: field: {}

  fields: [
    {
      name: "ip"
      req: true
      short: "The client's IP address"
      type: "`$STRING`"
    }
  ]

  name: "ip_address"
  op: {
    load: {
      name: "load"
      points: [
        {
          method: "GET"
          orig: "/ip/json"
          segments: [
            {
              lit: "ip"
            }
            {
              lit: "json"
            }
          ]
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }



}
